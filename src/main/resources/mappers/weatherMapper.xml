<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace = "com.ashspell.test.lesson05.dao.WeatherDAO">
 	<insert id  = "insertWeather" parameterType = "com.ashspell.test.lesson05.model.Weather">
 		INSERT INTO `weather_history`
 		(`date`
 		.`weather`
 		,`temperatures`
 		,`precipitation`
 		,`microDust`
 		,`windSpeed`
 		,`createdAt`
 		,`updatedAt`
 		) values (
 		
 		#{date}
 		,#{weather}
 		,#{temperatures}
 		,#{precipitation}
 		,#{microDust}
 		,#{windSpeed}
 		,now()
 		,now()
 		)
 		
 	</insert> 
 </mapper>